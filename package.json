{
  "name": "Deteksi-Hoaks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cp -r public dist",
    "start": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  }
}
